" we like line numbers
set number ruler

" utf8 everything
set encoding=utf8
set fileencoding=utf8

" sensible tabbing
set smarttab
set expandtab
set tabstop=4
set shiftwidth=4

" remove the menubar
if has("gui_running")
   set guioptions-=T
endif

" nice colour scheme
colorscheme molokai
